# ── Web framework ──────────────────────────────────────────────────────────
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# ── Image processing ────────────────────────────────────────────────────────
# opencv-python-headless = no libGL.so.1 needed (required on headless servers)
opencv-python-headless>=4.8.0,<5.0.0
pillow>=10.0.0,<12.0.0

# ── NumPy: cap below 2.0 — ultralytics 8.1.x uses deprecated numpy 1.x APIs ─
numpy>=1.24.3,<2.0.0

# ── Deep learning ────────────────────────────────────────────────────────────
# Install torch from PyPI (no extra-index-url needed).
# These are CPU-capable by default; CUDA is optional and unused on Render.
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0

# ── YOLOv12 / Ultralytics ────────────────────────────────────────────────────
ultralytics==8.1.34
